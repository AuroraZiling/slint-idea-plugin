import { I18n, TranslationText } from "@i18n";
import { IconDisplay, IconSet } from "@lucide";
import { Shape } from "@shapes";
export component App inherits Window {
    width: 400px;
    height: 240px;
    in property <TranslationText> translations: I18n.text;
    VerticalLayout {
        Text { text: root.translations.hello; }
        Shape {}
        IconDisplay {}
    }
}
